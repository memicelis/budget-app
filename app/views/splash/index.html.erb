<div class="min-h-screen flex flex-col justify-between items-center max-w-screen">
  <h1 class="text-xl text-custom-bold my-auto">BUDGET MANAGER</h1>
  <div class="w-full flex flex-col space-y-2">
      <%= link_to 'LOG IN', new_user_session_path, class: 'w-full bg-blue-500 text-white hover:underline py-2 px-4 rounded text-center' %>
    <%= link_to 'SIGN UP', new_user_registration_path, class: 'w-full text-custom-normal hover:underline py-2 px-4 rounded text-center' %>
    <%= button_to 'SIGN OUT', destroy_user_session_path, method: :delete, class: 'w-full bg-red-500 text-white py-2 px-4 rounded' %>
  </div>
</div>

